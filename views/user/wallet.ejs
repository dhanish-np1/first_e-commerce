<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wonderful Wallet Balance</title>
    <style>
        .wallet-container {
            background-color: rgb(255, 255, 255);
            padding-top: 50px;
            padding-bottom: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }
        .wallet-card {
            border-radius: 15px;
            height: 300px;
            width: 400px;
            border: 1px solid #000000;
            box-shadow: 0px 5px 20px rgba(0, 0, 0, 0.2); /* Enhanced shadow */
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background-color: #f9f9f9; /* Light gray background */
            color: #333; /* Dark text color */
        }
        .balance {
            font-size: 32px;
            font-weight: bold;
            margin-bottom: 20px;
            color: #007bff; /* Blue color for balance */
        }
        .wallet-title {
            font-size: 24px;
            margin-bottom: 20px;
        }
    </style>
    <style>
        .transaction-display {
            width: 100%;
            max-width: 600px; /* Adjust as needed */
            margin: 0 auto;
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            background-color: #ffffff;
            overflow-y: auto; /* Enable vertical scrollbar when needed */
            max-height: 400px; /* Set a maximum height and enable scrolling */
        }

        /* Style for individual transaction items */
        .transaction-item {
            margin-bottom: 10px;
            padding: 10px;
            border-radius: 8px;
            background-color: #f0f0f0;
        }
        .transaction-display {
    /* Other styles */
    overflow-y: auto; /* Enable vertical scrollbar when needed */
    max-height: 400px; /* Set a maximum height and enable scrolling */
}
    </style>
</head>
<body>
    <div class="wallet-container">
        <div class="wallet-card">
            <h2 class="wallet-title">My Wallet balance</h2>
            <p class="balance">Rs:&#8377;<%=userData.wallet%></p>
            <!-- You can customize the content of the wallet card as needed -->
        </div>
        
        
    </div>
    <div class="transaction-display" style="margin-bottom: 25px;">
        <!-- Sample transactions, replace with actual data -->
        <% walletHistory.forEach(function(transaction) { %>
            <div class="transaction-item" style="color: black;">
                <p>Amount:Rs: &#8377; <%=transaction.amount%> </p>
                <p>Date: <%= transaction.date.toISOString().slice(0, 10) %></p>
                <p>Description: <%=transaction.reason%></p>
                <% if (transaction.reason=='no reason') { %>
                    <p style="color: green;"> Amount Credited</p>
                <% } else { %>
                    <p style="color: red;"> Amount Debited</p>
                <% } %>
            </div>
        <% }); %>
        
    </div>
</body>
</html>
